import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  layoutContent: {
    padding: 0,
  },
  currencySelectorContainer: {
    padding: 32,
    borderRadius: 16,
    backgroundColor: 'white',
    gap: 8,
  },
  currencyRateChartContainer: {
    marginTop: 8,
    padding: 16,
    backgroundColor: 'white',
    borderRadius: 16,
  },
});
