import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flexGrow: 1,
    backgroundColor: '#F0F0F0',
  },
  scrollViewContainer: {
    flexGrow: 1,
    padding: 16,
  },
});
